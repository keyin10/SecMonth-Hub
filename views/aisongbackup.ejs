<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>AI Song Upload</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    .main-container {
      max-width: 1000px;
      margin: 2rem auto;
      padding: 1rem;
    }
    .card {
      background: white;
      border-radius: 10px;
      padding: 1.5rem;
      box-shadow: 0 4px 8px rgb(0 0 0 / 0.1);
      margin-bottom: 2rem;
    }
    .song-gallery {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      margin-top: 1rem;
    }
    .gallery-item {
      flex: 1 1 250px;
      min-width: 250px;
      max-width: 300px;
      background: #f9f9f9;
      border-radius: 10px;
      padding: 1rem;
      box-shadow: 0 2px 4px rgb(0 0 0 / 0.1);
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      justify-content: center;
      text-align: center;
    }
    form input[type="text"],
    form input[type="file"] {
      width: 100%;
      padding: 0.4rem 0.6rem;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
    }
    form button.btn-primary {
      background: linear-gradient(135deg, #667eea, #764ba2);
      border: none;
      color: white;
      padding: 0.7rem 1.4rem;
      font-size: 1.1rem;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    form button.btn-primary:hover {
      background: linear-gradient(135deg, #564ec1, #613a93);
    }
  </style>
</head>

<%- include('partials/header') %>

<body>
<div class="main-container">

  <!-- Tips Card -->
  <div class="card">
    <h2>🎵 Tips for Creating AI-Generated Music</h2>
    <p>Here are some ideas to help you get started:</p>
    <ul>
      <li>Use AI tools like <a href="#" target="_blank">Find Music AI</a> or <a href="https://www.aiva.ai/" target="_blank">AIVA</a></li>
      <li>Think security themes: cyber beats, data protection rhythms, hacker vibes</li>
      <li>Keep your audio file under 30MB and in MP3 format</li>
    </ul>
  </div>

  <!-- Upload Form -->
  <div class="card">
    <h2>🎶 Upload Your AI Song</h2>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-danger" style="padding: 0.6rem; margin-bottom: 1rem; border-radius: 6px; background-color: #ffe5e5; color: #a33;">
        <%= error %>
      </div>
    <% } %>

    <% if (typeof success !== 'undefined' && success) { %>
      <div class="alert alert-success" style="padding: 0.6rem; margin-bottom: 1rem; border-radius: 6px; background-color: #e5ffe5; color: #2a7;">
        <%= success %>
      </div>
    <% } %>

    <form action="/aisongs" method="POST" enctype="multipart/form-data">
      <label for="title">Title:</label><br />
      <input id="title" type="text" name="title" placeholder="e.g., Cyber Beats" required /><br /><br />

      <label for="song">Song (MP3):</label><br />
      <input id="song" type="file" name="song" accept=".mp3" required /><br /><br />

      <button class="btn btn-primary" type="submit">Upload Song</button>
    </form>
  </div>

<div class="card">
  <h3>🎶 Song Gallery</h3>
  <div style="display: grid; gap: 1rem;">
    <% songs.forEach(song => { %>
      <div style="display: flex; justify-content: space-between; align-items: center; padding: 1rem; background: linear-gradient(45deg, #f8f9ff, #e6f2ff); border-radius: 10px;">
        <div>
          <strong>🎵 <%= song.title %></strong><br />
          <small>by <%= song.submitted_by %></small>
        </div>
        <audio controls style="width: 180px; height: 30px;">
          <source src="<%= song.path %>" type="audio/mpeg">
          Your browser does not support the audio element.
        </audio>
      </div>
    <% }) %>
  </div>
</div>

</div>
</body>
</html>
