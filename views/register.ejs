<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Register - SOTI Security Month 2025</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
/* Register Page Styles */
  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: #333;
    font-size: 0.95rem;
  }

  .form-input {
    width: 100%;
    padding: 1rem 1.2rem;
    border: 2px solid rgba(102, 126, 234, 0.1);
    border-radius: 15px;
    font-size: 1rem;
    background: rgba(255, 255, 255, 0.8);
    transition: all 0.3s ease;
    outline: none;
  }

  .form-input:focus {
    border-color: #667eea;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    background: rgba(255, 255, 255, 0.95);
  }

  .form-input:hover {
    border-color: rgba(102, 126, 234, 0.3);
  }

  .error-message {
    background: linear-gradient(45deg, #ffebee, #ffcdd2);
    color: #c62828;
    padding: 1rem;
    border-radius: 15px;
    margin-bottom: 1.5rem;
    border-left: 4px solid #f44336;
    font-weight: 500;
    animation: slideIn 0.3s ease;
  }

  @keyframes slideIn {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
        .register-container {
            max-width: 450px;
            margin: 4rem auto;
            padding: 2rem;
        }
        
        .register-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 25px;
            padding: 3rem;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            position: relative;
            overflow: hidden;
        }
        
        .register-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            border-radius: 25px 25px 0 0;
        }
        
        .register-title {
            text-align: center;
            font-size: 2.2rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 0.5rem;
            background: linear-gradient(45deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .register-subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 2rem;
            font-size: 1rem;
        }
        
        .register-btn {
            width: 100%;
            padding: 1rem;
            font-size: 1.1rem;
            font-weight: bold;
            margin-top: 1rem;
            position: relative;
            overflow: hidden;
        }
        
        .register-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        
        .register-btn:hover::before {
            left: 100%;
        }
        
        .success-message {
            background: linear-gradient(45deg, #e8f5e8, #c8e6c9);
            color: #2e7d32;
            padding: 1rem;
            border-radius: 15px;
            margin-bottom: 1.5rem;
            border-left: 4px solid #4caf50;
            font-weight: 500;
            animation: slideIn 0.3s ease;
        }
        
        .login-link {
            text-align: center;
            margin-top: 2rem;
            color: #666;
        }
        
        .login-link a {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }
        
        .login-link a:hover {
            color: #764ba2;
        }
        
        .welcome-icon {
            text-align: center;
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.8;
        }  
  </style>
</head>
<body>
  <header class="header">
    <div class="header-content">
      <a style="text-decoration: none;" href="/" class="logo">🛡️  SOTI Cybersecurity Awareness Month (CAM)</a>
        <div class="user-info">
        <a href="/login" class="btn btn-primary">Login</a>
      </div>
    </div>
  </header>


  <main class="register-container">
    <div class="register-card">
      <h2 class="register-title">Join the Fun</h2>
      <p class="register-subtitle">Use your work email to your account to start your security journey</p>
      
      <% if (error) { %>
        <div class="error-message">
          <%= error %>
        </div>
      <% } %>
      
      <% if (success) { %>
        <div class="success-message">
          <%= success %>
        </div>
      <% } %>
      
      <form method="POST" action="/register">
        <div class="form-group">
          <label class="form-label" for="email">Email Address</label>
          <input
            type="email"
            id="email"
            name="email"
            class="form-input"
            required
            value="<%= email %>"
            placeholder="Email"
          />
        </div>
        
        <div class="form-group">
          <label class="form-label" for="password">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            class="form-input"
            required
            placeholder="Password"
          />
        </div>
        
        <button type="submit" class="btn btn-primary register-btn">
          Create Account
        </button>
      </form>
      
      <div class="login-link">
        Already have an account? <a href="/login">Sign in here</a>
      </div>
    </div>
  </main>
</body>
</html>
